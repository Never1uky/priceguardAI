# P0.3 — Privacy CLAIMED vs ACTUAL

**Дата:** 2026-08-20 (revalidate после P0.1 funnel)  
**Policy:** v2.4 — `docs/PRIVACY_POLICY_RU.md` / EN; landing SPA: `priceguard-landing` `PrivacyPage.tsx` (`VERSION = '2.4'`); live: https://priceguard-landing.vercel.app/privacy  
**Статус:** **REVALIDATED (audit DONE)**  
**Правило:** этот документ — audit only. **Не** менять Privacy Policy / CWS Data Use без отдельного approve.

## Verdict

**CLAIMED ≈ ACTUAL** после P0.1. Product funnel — **local ring only** (INFO); remote funnel disclosure **не** требуется, пока default-off. Policy / landing HTML / CWS Data Use **не** меняли. Copy drift «unlimited tracking» → **P0.4** (не privacy policy).

## Матрица

| Category | CLAIMED (policy / CWS) | ACTUAL (code) | Match? |
|----------|------------------------|---------------|--------|
| Website content (MP cards) | Читает WB/Ozon/YM для цены/сравнения | Content scripts + HiddenBrowser + parsers; Scrappey server-side for Premium unlocker / cron | OK |
| Product titles, URLs, prices | Обрабатываются локально + cloud if signed in | `chrome.storage`; `tracked-sync` / `compare-sync`; shared caches | OK |
| Reviews → AI | Выборка отзывов → Edge AI proxy → Grok/OpenAI/Perplexity | `ai-proxy`, `product-cache`; keys not in extension | OK |
| Personal account | Email via Supabase Auth optional | Auth in popup; JWT to Edge | OK |
| device_id | Random UUID for limits/license | Local storage | OK |
| Telegram chat id | Optional; for alerts | `sync-alert-settings`; bots Alerts + Support | OK |
| Payments | YooKassa; no card data in extension | `create-payment` / webhook; return URL landing | OK |
| Telemetry (pipeline) | Local ring; opt-in remote WARN/ERROR; search-metrics truncated | `docs/TELEMETRY.md`; `log.ts` remote только warn/error + `remoteEnabled` | OK |
| **Product funnel (P0.1)** | Не отдельный продукт в policy; под «телеметрия на устройстве» | `src/lib/telemetry/funnel.ts` → `telemetry.info` (local ring). Payload: marketplace enum, `is_first`, outcome, plan id, `ok`, `source` — **без** URL, titles, reviews, chat id, email, license keys. Article только fingerprint для local dedupe card-open. Remote INFO **не** шлётся (`remoteEnabled` default false; remote queue only WARN/ERROR) | **OK** |
| Support auto-report | Truncated error ≤800, context, version, userId | `support-report.ts` → `support-notify` | OK |
| Cookies of user browsing | Not collected | Scrappey: no browser cookies | OK |
| Passwords / AI prompts in logs | Never | `redact.ts` drops prompt/password/token/email | OK |
| Cross-market mapping | Shared non-personal mapping | `cross-market-map`, match-feedback | OK |
| SEO site | Separate product pages | `seo_product_pages`; CTA to extension | OK (separate product) |
| Free/Premium limits in CWS copy | Free 5 / Premium tracking | Code: Premium **50** tracked; listing EN/RU «unlimited tracking» | **DRIFT** → P0.4 (copy, not policy) |

## Что уходит наружу (кратко)

| Destination | Data |
|-------------|------|
| Supabase Edge | AI payloads, sync lists, alerts, payments, search-metrics, opt-in telemetry WARN/ERROR, Scrappey via server |
| AI providers | Via Edge only: reviews sample + product fields |
| Scrappey | Product URL scrape server-side |
| Telegram API | Alert messages (title, prices, product URL+referral), support ops |
| YooKassa | Via Edge create-payment; webhook re-fetch |
| **Funnel events** | **Никуда** (local ring), пока remote INFO не включат отдельно |

## Что остаётся локально

Tracked/compare drafts, SERP cache, telemetry ring (incl. funnel INFO), funnel flags / card-open dedupe, AI daily counters, device id, onboarding flag, pending payment session, UI theme.

## Gaps / follow-ups

### Закрыто (после P0.1)

- Funnel remote counters / disclosure — **не нужно**, пока funnel только local + remote INFO default-off.
- Matrix обновлена: строка Product funnel + ссылки на `funnel.ts` / `TELEMETRY.md`.

### MANUAL (владелец)

1. **CWS Data Use** — сверка с матрицей при каждом upload (без смены формы в этом шаге).  
2. **Live Privacy** — SPA `/privacy` = condensed v2.4 (`PrivacyPage.tsx`); полный текст — `docs/PRIVACY_POLICY_RU.md`. Убедиться, что production landing задеплоен с `VERSION = '2.4'` (исходник в репо landing — да).  
3. **CWS listing** «unlimited tracking» → «до 50» — **P0.4**, ждать разблокировки listing после review 0.9.95.  
4. **Шаг 4 growth (GSC indexing)** — не privacy; отдельно (не закрыт).

### Не блокер / known

- Pipeline match telemetry может класть `title` в WARN data → `redact` режет до ≤80; в funnel **нет** titles. Менять policy не требуется.  
- Landing Privacy короче полного markdown docs — исторически; sync copy только с approve.

## Не уходит наружу (контроль)

- Full page HTML dumps as product analytics  
- Other sites’ cookies  
- User passwords  
- Raw card PAN  
- Funnel URLs / chat ids / license keys  

## Код-фикс в этом шаге

**Нет** — реального privacy DRIFT, требующего redact-fix, не найдено. Policy / CWS Data Use не трогали.
