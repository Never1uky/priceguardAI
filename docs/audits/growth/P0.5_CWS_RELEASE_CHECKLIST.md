# CWS release checklist — v0.9.106+ (pre-CWS safety)

**Цель:** выложить в Chrome Web Store сборку с multi-MP opt-in + Phase 2 cost/disclosure fixes.  
**Целевая версия:** **0.9.107** (сверить `package.json` + `manifest.json` + `dist/manifest.json` после `package:zip`).  
**AGENT:** артефакт + чеклист. **MANUAL:** upload / Submit for review / Privacy HTML на лендинге.

## Что должно быть в сборке

| Area | Expectation |
|------|-------------|
| Defaults | «Где искать» = WB+Ozon+YM; test MPs opt-in only |
| Migration | eldorado → mvideo; old users keep trio |
| Scrappey TG | `update-prices` = tracked core only; compare backup `skipUnlocker`; tracked client backup `skipUnlocker` |
| compare-research | Scrappey verify = Premium only; targets ∩ selected |
| Unlocker | Premium + core + selected; prefers shared cache (`skipCache: false`) |
| SEO | new MPs wired, `publishAllowed: false` |
| Disclosure | Privacy 2.5 + CWS listing + hosts justifications for opt-in MPs |
| Production manifest | no `localhost` in `externally_connectable` (stripped at build) |

## Артефакт

```bash
npm run package:zip
node scripts/validate-zip.mjs   # if present
# Check dist/manifest.json version + externally_connectable (no localhost)
```

Zip: `priceguard-ai-v{version}.zip` in project root (path from script output).

## What's New (черновик)

См. `docs/CHROME_WEB_STORE_LISTING.md` §2b — обновить под релизный patch.

RU (ориентир):

```
• Сравнение: дополнительные магазины opt-in (Мегамаркет, AliExpress, М.Видео, DNS, Ситилинк, Lamoda)
• По умолчанию по-прежнему WB / Ozon / Я.Маркет
• Безопаснее фоновые проверки цен (без лишнего Scrappey на backup)
• Privacy / описание CWS приведены к фактическим host permissions
```

## Operator checklist (MANUAL)

- [ ] `package.json` version === `manifest.json` === zip / `dist/manifest.json`
- [ ] Smoke: new install → defaults trio
- [ ] Smoke: existing user upgrade → selection unchanged (не 9 MP)
- [ ] Smoke: opt-in megamarket → compare; disable → no research for it
- [ ] Smoke: Free tracked refresh → no unexpected Scrappey (Premium unlocker)
- [ ] Smoke: Premium research → core verify still works
- [ ] Smoke: WB+Ozon only → Edge research не таргетит YM (если YM deselected)
- [ ] Privacy URL live: https://priceguard-landing.vercel.app/privacy (задеплоить `PRIVACY_POLICY_RU` / HTML mirror)
- [ ] CWS §9 host justifications pasted for new hosts
- [ ] Single purpose + short description updated in Dashboard
- [ ] Upload zip → Submit for review
- [ ] After approve: Unlisted smoke → Public

## Не делать в этом релизе

- Flip SEO `publishAllowed` for new MPs  
- Expand `update-prices` / Scrappey to tab-tier MPs  
- Remote telemetry default-on  
- Schema migration for synced search selection  

## Риски

- CWS review: new host_permissions (mitigated by disclosure)  
- Ozon antibot (known)  
- Landing privacy HTML may lag markdown until manual deploy  

## Статус AGENT

Phase 2 code + docs in repo. Upload / live Privacy / real TG — **только владелец**.
